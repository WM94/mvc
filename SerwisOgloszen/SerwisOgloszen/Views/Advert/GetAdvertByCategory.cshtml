@model  IEnumerable<DAL.ViewModel.Custom.AdvertWithUserViewModel>

 
<table class="table">
    @*<tr>
       
        <th>
           @Html.DisplayNameFor(  model => model.FirstOrDefault().ogloszenie.id)
   
        </th>
        <th>
            @Html.DisplayNameFor(  model => Model.FirstOrDefault().ogloszenie.Cena)
        </th>

        <th>
            @Html.DisplayNameFor(  model => Model.FirstOrDefault().ogloszenie.Nazwa)
        </th>

        <th>
            @Html.DisplayNameFor( model => Model.FirstOrDefault().user.Imie)
            @Html.DisplayNameFor(  model => Model.FirstOrDefault().user.Nazwisko)
        </th>

        <th></th>
    </tr>*@

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ogloszenie.id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ogloszenie.Cena)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ogloszenie.Nazwa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ogloszenie.Opis)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user.Imie)
                @Html.DisplayFor(modelItem => item.user.Nazwisko)
            </td>
            <td>
            
            </td>
        </tr>
    }

</table>